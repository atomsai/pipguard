#!/usr/bin/env bash
set -euo pipefail

echo "Building fixture wheels is not required for testing."
echo "Fixtures are provided as unpacked directories."
echo "To create wheel fixtures, use: python -m build <fixture-dir>"
